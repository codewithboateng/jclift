database:
  driver: sqlite
  dsn: ./jclift.db

analysis:
  sources: ["./samples/bank-small"]
  mips_to_usd: 250

reporting:
  out_dir: ./reports

logging:
  format: json
  level: info

rules:
  severity_threshold: LOW # or MEDIUM/HIGH
  disable: [] # e.g. ["DD-DUPLICATE-DATASET"]
  sortwk:
    primary_cyl_threshold: 500 # tune per site
